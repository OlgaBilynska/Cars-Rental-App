import { createAsyncThunk } from "@reduxjs/toolkit";

export const getCars = createAsyncThunk(
    'cars/getCars',
    async ({ }, thunkAPI) => {
        try {
            const queryParams = [];
        }
    }
)
